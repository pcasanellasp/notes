{
  "name": "learn-docker",
  "version": "1.0.0",
  "description": "Learn Docker",
  "main": "index.js",
  "scripts": {
    "build": "docker build -t learn-docker .",
    "build:server": "docker build -t learn-docker .",
    "serve": "docker run --rm -it -p 3001:3000 learn-docker",
    "server": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.10.13"
  }
}
